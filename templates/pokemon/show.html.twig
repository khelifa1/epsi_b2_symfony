{% extends 'base.html.twig' %}

{% block title %}Pokemon{% endblock %}

{% block body %}
    <h1>Pokemon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pokemon.id }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ pokemon.createdAt }}</td>
            </tr>
            <tr>
                <th>Update_at</th>
                <td>{{ pokemon.UpdateAt }}</td>
            </tr>
            <tr>
                <th>Deleted_at</th>
                <td>{{ pokemon.deletedAt }}</td>
            </tr>
            <tr>
                <th>Isactive</th>
                <td>{{ pokemon.isactive }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pokemon.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ pokemon.type }}</td>
            </tr>
            <tr>
                <th>Pv</th>
                <td>{{ pokemon.Pv }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pokemon_index') }}">back to list</a>

    <a href="{{ path('pokemon_edit', {'id': pokemon.id}) }}">edit</a>

    {{ include('pokemon/_delete_form.html.twig') }}
{% endblock %}
